name: basic
object:
  _target_: modules.loader.create_dataloaders
  train_size: 0.8
  test_size: 0.0
  batch_size: 128
  balanced: True
  multiplier: 1
  seed: ${training.seed}
  num_workers: 4
  persistent_workers: True

